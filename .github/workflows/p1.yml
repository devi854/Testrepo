name: Project-1

on:
  
  pull_request:
    branches:
      - main
    paths:
      - 'Project-1/**'

  

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v3.0.3
        with:
          dotnet-version: '6.0'
    
        
        
     

